import { FAQContent } from "@/components/faq-content";

export const metadata = {
  title: "FAQ — Asli",
  description:
    "Honest answers about how Asli works, who pays, what the satellite actually verifies, and where we are in the journey.",
};

export default function FAQPage() {
  return <FAQContent lang="en" />;
}
